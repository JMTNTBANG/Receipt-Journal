import { Box, Typography } from "@mui/material";

export default function BackupPage() {
    return (
        <Box sx={{ flex: 1, display: 'flex', flexDirection: 'column', minHeight: 0, padding: "20px" }}>
            <Typography variant="h3">Backup Page</Typography>
            <Typography variant="body1" sx={{mt: 2}}>
                Coming Soon!
            </Typography>
        </Box>
    );
}
